%{
#include "y.tab.h"
#include <stdlib.h>
%}

%%
[0-9]+      { yylval = atoi(yytext); return NUMBER; }
[+\-*/()]   { return yytext[0]; }
\n          { return '\n'; }
[ \t]       ;  /* ignore spaces/tabs */
.           { printf("Invalid character: %s\n", yytext); }
%%

int yywrap() { return 1; }
